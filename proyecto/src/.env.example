# Database Configuration
DB_ROOT_PASSWORD=root_password
DB_USER=pulso_user
DB_PASSWORD=pulso_password
DB_NAME=pulso_solidario

# Application Configuration
APP_ENV=development
APP_DEBUG=true
# URL pública del frontend (enlaces de confirmación de correo)
APP_URL=http://localhost:3000

# Correo (Mailhog en local)
SMTP_HOST=mailhog
SMTP_PORT=1025
MAIL_FROM=noreply@pulso-solidario.local

PHPMYADMIN_USER=pulso_user
PHPMYADMIN_PASS=pulso_password
